# ⑧ インデントはスペースのみ（タブ禁止）
key: value
"aaaa": "bbbb"
111: 11111
special_chars: special_chars:aa
aaa: 'aaaa''a'''
double quotes: "\u263A"

array:
  - aaa
  - aaa


object:
  "aaaa": ffff
  key: value

json array: [aaaa,aaa,aaa]

aa: null


object2:
  aaaa: ffff
  key: value
  aaaaa:
    bbbb: bbbb




object3:
  - a
  - b
  - c
  - aaa1:
      aaa2: aaa
  - a


# - xxx → リスト（配列）
# key: value → オブジェクトのプロパティ（マップ）
# 👉 この理解で OK

text: |
  これは1行目
  これは2行目
  これは3行目
# YAML の パイプ | は、「改行をそのまま保持する複数行文字列」 を作るための記号。


run: |
  echo "start testing"
  go mod tidy
  go test ./...
  echo "done"


data:
  init.sql: |
    CREATE TABLE users (
      id SERIAL PRIMARY KEY,
      name TEXT
    );


# >（折りたたみブロック）は、
# “改行をスペースに畳んで、1 行のテキストとして扱いたい時” に使われる。
text1: >
  これは1行目
  これは2行目
  これは3行目

# ⑪ null の書き方は3つ
a: null
b: ~
c:

spec:
  replicas: 2
  template:
    spec:
      containers:
        - name: api
          image: example:latest
          env:
            - name: MESSAGE
              value: >
                このサービスは内部APIです。
                外部公開はされません。

# > や | は「見た目の YAML と、実際の値（プログラムに渡される文字列）が違う」
# ＝ YAML 側で “見やすさのために書いた改行を、内部表現に変換する” 仕組み。